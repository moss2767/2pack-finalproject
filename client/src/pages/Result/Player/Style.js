import { makeStyles } from '@material-ui/core'

const useStyles = makeStyles(() => ({
  result: {
    margin: '1rem 2rem'
  },
  text: {
    width: '30rem',
    marginLeft: '2rem'
  },
  progressBar: {
    margin: '2rem 2rem',
    height: '2rem',
    borderRadius: '.5rem'
  }
}))

export default useStyles
